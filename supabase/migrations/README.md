# Migracoes

Vazio nesta etapa. Nenhum esquema foi definido: a modelagem depende da SPEC de
persistencia, ainda nao escrita.

Convencao quando comecarem: `YYYYMMDDHHMMSS_descricao_curta.sql`, uma migracao
por mudanca, sempre para frente (nunca editar migracao ja aplicada).

Ver ADR-003 para a decisao de usar PostgreSQL via Supabase e o que ela implica
em Row Level Security.
